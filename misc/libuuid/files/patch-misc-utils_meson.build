--- misc-utils/meson.build.orig	2024-11-26 14:28:50 UTC
+++ misc-utils/meson.build
@@ -162,11 +162,13 @@ getopt_sources = files(
   'getopt.c',
 )
 
-install_data(
-  'getopt-example.bash',
-  'getopt-example.tcsh',
-  install_dir : docdir,
-  install_mode: 'rwxr-xr-x')
+if not get_option('build-getopt').disabled()
+  install_data(
+    'getopt-example.bash',
+    'getopt-example.tcsh',
+    install_dir : docdir,
+    install_mode: 'rwxr-xr-x')
+endif
 
 exch_sources = files(
   'exch.c',
